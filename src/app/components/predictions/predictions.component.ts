import { Component, OnInit, inject } from '@angular/core';
import { CommonModule } from '@angular/common';
import { FormsModule } from '@angular/forms';
import { finalize, forkJoin, switchMap } from 'rxjs';
import { LucideAngularModule } from 'lucide-angular';
import { AuthService } from '../../services/auth.service';
import { PredictionDashboard, PredictionHorizon, SupplyRecommendation } from '../../models/prediction-dashboard.model';
import { PredictionDashboardService } from '../../services/prediction-dashboard.service';

@Component({
  selector: 'app-predictions', standalone: true,
  imports: [CommonModule, FormsModule, LucideAngularModule],
  template: `
  <div class="space-y-6">
    <header class="flex flex-col gap-4 xl:flex-row xl:items-end xl:justify-between"><div><p class="text-xs font-bold uppercase tracking-wider text-brand-terra">Modelo predictivo</p><h2 class="mt-1 text-2xl font-bold text-brand-dark">Predicciones de consumo</h2><p class="text-gray-500">Demanda estimada de insumos para los próximos 3, 7 y 14 días.</p></div><div *ngIf="isAdmin" class="flex flex-wrap items-end gap-3"><label class="text-sm text-gray-600">Fecha a procesar<input [(ngModel)]="executionDate" type="date" class="mt-1 block rounded-lg border border-gray-200 px-3 py-2 text-brand-dark"></label><label class="text-sm text-gray-600">Margen<input [(ngModel)]="safetyMargin" type="number" min="0" max="1" step="0.01" class="mt-1 block w-24 rounded-lg border border-gray-200 px-3 py-2 text-brand-dark"></label><button type="button" (click)="runPrediction()" [disabled]="running || !executionDate" class="inline-flex items-center gap-2 rounded-lg bg-brand-terra px-4 py-2.5 text-sm font-bold text-white disabled:opacity-50"><lucide-icon name="brain-circuit" [size]="17"></lucide-icon>{{ running ? 'Procesando...' : 'Ejecutar ETL y predecir' }}</button></div></header>
    <div *ngIf="isAdmin" class="flex gap-3 border border-brand-terra/20 bg-brand-cream/40 px-4 py-3 text-sm text-brand-dark"><lucide-icon name="activity" [size]="18" class="shrink-0 text-brand-terra"></lucide-icon><p>El proceso toma los datos operativos de la fecha elegida, ejecuta el ETL, consulta el modelo FastAPI y guarda las predicciones.</p></div>
    <div *ngIf="error" class="flex items-center justify-between gap-3 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700"><span>{{ error }}</span><button type="button" (click)="error = ''" aria-label="Cerrar">×</button></div>
    <div *ngIf="success" class="rounded-lg border border-green-200 bg-green-50 px-4 py-3 text-sm text-green-700">{{ success }}</div>
    <section class="flex flex-col gap-3 border-y border-brand-wood/10 py-4 lg:flex-row lg:items-center lg:justify-between"><label class="text-sm font-medium text-gray-600">Resultados guardados<select [(ngModel)]="selectedDate" (ngModelChange)="load()" class="ml-2 rounded-lg border border-gray-200 bg-white px-3 py-2 text-brand-dark"><option *ngFor="let date of dates" [value]="date">{{ formatDate(date) }}</option></select></label><div class="flex items-center gap-2"><span class="text-sm text-gray-600">Horizonte:</span><button *ngFor="let horizon of horizons" type="button" (click)="selectHorizon(horizon)" class="rounded-lg border px-3 py-1.5 text-sm" [ngClass]="selectedHorizon === horizon ? 'border-brand-terra bg-brand-terra text-white' : 'border-gray-200 bg-white text-gray-600'">{{ horizon }} días</button></div></section>
    <section *ngIf="data as dashboard; else empty" class="space-y-6">
      <div class="grid grid-cols-1 gap-4 md:grid-cols-2 xl:grid-cols-4"><article class="border border-brand-wood/10 bg-white p-5 shadow-sm"><p class="text-sm text-gray-500">Insumos visibles</p><p class="mt-2 text-3xl font-bold text-brand-dark">{{ filteredRows.length }}</p><p class="mt-2 text-xs text-gray-500">de {{ dashboard.recomendaciones.length }} registros</p></article><article class="border border-brand-wood/10 bg-white p-5 shadow-sm"><p class="text-sm text-gray-500">Requieren compra</p><p class="mt-2 text-3xl font-bold text-red-600">{{ requiresPurchase }}</p><p class="mt-2 text-xs text-gray-500">Para {{ selectedHorizon }} días</p></article><article class="border border-brand-wood/10 bg-white p-5 shadow-sm"><p class="text-sm text-gray-500">Insumos críticos</p><p class="mt-2 text-3xl font-bold text-red-700">{{ criticalCount }}</p><p class="mt-2 text-xs text-gray-500">Riesgo igual o mayor a {{ riskThreshold }}%</p></article><article class="border border-brand-wood/10 bg-white p-5 shadow-sm"><p class="text-sm text-gray-500">Mayor demanda</p><p class="mt-2 truncate text-xl font-bold text-brand-dark">{{ topName }}</p><p class="mt-2 text-xs text-gray-500">{{ topRow ? (predictionValue(topRow) | number:'1.0-2') + ' ' + topRow.unidad : '' }}</p></article></div>
      <section class="border border-brand-wood/10 bg-white shadow-sm"><header class="flex flex-col gap-4 border-b border-gray-100 p-5"><div><h3 class="font-bold text-brand-dark">Detalle de predicciones</h3><p class="text-sm text-gray-500">Modelo {{ dashboard.version_modelo }} · {{ formatDate(dashboard.fecha_decision) }}</p></div><div class="flex flex-wrap items-end gap-2"><label class="text-xs font-medium text-gray-500">Insumo específico<select [(ngModel)]="selectedSupplyId" class="mt-1 block rounded-lg border border-gray-200 px-3 py-2 text-sm text-brand-dark"><option [ngValue]="null">Todos los insumos</option><option *ngFor="let item of supplies" [ngValue]="item.id_insumo">{{ item.nombre_insumo }} · {{ item.unidad }}</option></select></label><label class="text-xs font-medium text-gray-500">Buscar<input [(ngModel)]="search" type="search" placeholder="Nombre" class="mt-1 block rounded-lg border border-gray-200 px-3 py-2 text-sm"></label><label class="text-xs font-medium text-gray-500">Categoría<select [(ngModel)]="category" class="mt-1 block rounded-lg border border-gray-200 px-3 py-2 text-sm"><option value="">Todas</option><option *ngFor="let item of categories" [value]="item">{{ item }}</option></select></label><label class="text-xs font-medium text-gray-500">Estado<select [(ngModel)]="state" class="mt-1 block rounded-lg border border-gray-200 px-3 py-2 text-sm"><option value="">Todos</option><option value="CRITICO">Críticos</option><option value="REQUIERE_COMPRA">Requiere compra</option><option value="STOCK_SUFICIENTE">Stock suficiente</option></select></label><label class="text-xs font-medium text-gray-500">Umbral de riesgo<input [(ngModel)]="riskThreshold" type="number" min="1" max="100" step="1" class="mt-1 block w-24 rounded-lg border border-gray-200 px-3 py-2 text-sm"></label><button type="button" (click)="clearFilters()" class="rounded-lg border border-gray-200 px-3 py-2 text-sm text-gray-600">Limpiar</button><button type="button" (click)="exportCsv()" class="inline-flex items-center gap-2 rounded-lg border border-brand-wood/20 px-3 py-2 text-sm font-medium text-brand-wood"><lucide-icon name="file-text" [size]="16"></lucide-icon>CSV</button></div></header><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-brand-cream/40 text-xs uppercase text-gray-500"><tr><th class="px-4 py-3">Insumo</th><th class="px-4 py-3">Categoría</th><th class="px-4 py-3">Stock</th><th class="px-4 py-3">3 días</th><th class="px-4 py-3">7 días</th><th class="px-4 py-3">14 días</th><th class="px-4 py-3">Deficiencia</th><th class="px-4 py-3">Riesgo</th><th class="px-4 py-3">Estado</th></tr></thead><tbody><tr *ngFor="let item of filteredRows" class="border-t border-gray-100"><td class="px-4 py-3"><strong class="text-brand-dark">{{ item.nombre_insumo }}</strong><span class="block text-xs text-gray-500">{{ item.unidad }}</span></td><td class="px-4 py-3">{{ item.categoria_insumo || 'Sin categoría' }}</td><td class="px-4 py-3">{{ item.stock_actual | number:'1.0-2' }}</td><td class="px-4 py-3" [class.font-bold]="selectedHorizon === 3">{{ item.prediccion_3_dias | number:'1.0-2' }}</td><td class="px-4 py-3" [class.font-bold]="selectedHorizon === 7">{{ item.prediccion_7_dias | number:'1.0-2' }}</td><td class="px-4 py-3" [class.font-bold]="selectedHorizon === 14">{{ item.prediccion_14_dias | number:'1.0-2' }}</td><td class="px-4 py-3 font-medium">{{ item.compra_recomendada | number:'1.0-2' }} {{ item.unidad }}</td><td class="px-4 py-3"><span [ngClass]="isCritical(item) ? 'font-bold text-red-700' : 'text-gray-600'">{{ riskPercent(item) | number:'1.0-0' }}%</span></td><td class="px-4 py-3"><span class="rounded-full px-2 py-1 text-xs font-medium" [ngClass]="isCritical(item) ? 'bg-red-100 text-red-800' : item.estado === 'REQUIERE_COMPRA' ? 'bg-amber-50 text-amber-700' : 'bg-green-50 text-green-700'">{{ isCritical(item) ? 'Crítico' : item.estado === 'REQUIERE_COMPRA' ? 'Requiere compra' : 'Stock suficiente' }}</span></td></tr><tr *ngIf="!filteredRows.length"><td colspan="9" class="px-4 py-10 text-center text-gray-500">No hay insumos que coincidan con los filtros.</td></tr></tbody></table></div></section>
    </section>
    <ng-template #empty><section class="border border-dashed border-brand-wood/20 bg-white px-6 py-16 text-center"><lucide-icon name="brain-circuit" [size]="40" class="mx-auto text-brand-terra"></lucide-icon><h3 class="mt-3 font-bold text-brand-dark">{{ loading ? 'Cargando predicciones...' : 'No hay predicciones disponibles' }}</h3><p class="mt-1 text-sm text-gray-500">{{ loading ? 'Consultando los resultados guardados.' : 'Ejecuta el proceso predictivo para generar resultados.' }}</p></section></ng-template>
  </div>`
})
export class PredictionsComponent implements OnInit {
  private readonly api = inject(PredictionDashboardService); private readonly auth = inject(AuthService);
  data: PredictionDashboard | null = null; dates: string[] = []; selectedDate = ''; executionDate = ''; selectedHorizon: PredictionHorizon = 14; horizons: PredictionHorizon[] = [3, 7, 14]; safetyMargin = 0.1; loading = true; running = false; error = ''; success = ''; search = ''; category = ''; state = ''; selectedSupplyId: number | null = null; riskThreshold = 50;
  get isAdmin() { return this.auth.hasRole('ADMINISTRADOR'); }
  get supplies() { return [...(this.data?.recomendaciones ?? [])].sort((left, right) => left.nombre_insumo.localeCompare(right.nombre_insumo, 'es')); }
  get categories() { return [...new Set((this.data?.recomendaciones ?? []).map(item => item.categoria_insumo).filter(Boolean))].sort(); }
  get filteredRows() { const term = this.search.trim().toLowerCase(); return (this.data?.recomendaciones ?? []).filter(item => (!term || item.nombre_insumo.toLowerCase().includes(term)) && (this.selectedSupplyId === null || item.id_insumo === this.selectedSupplyId) && (!this.category || item.categoria_insumo === this.category) && this.matchesState(item)); }
  get requiresPurchase() { return this.filteredRows.filter(item => item.estado === 'REQUIERE_COMPRA').length; }
  get criticalCount() { return (this.data?.recomendaciones ?? []).filter(item => this.isCritical(item)).length; }
  get topRow() { return [...this.filteredRows].sort((a, b) => this.predictionValue(b) - this.predictionValue(a))[0]; }
  get topName() { return this.topRow ? this.topRow.nombre_insumo : 'Sin datos'; }
  clearFilters() { this.search = ''; this.category = ''; this.state = ''; this.selectedSupplyId = null; this.riskThreshold = 50; }
  ngOnInit() { this.api.getDates().subscribe({ next: dates => { this.dates = dates; this.selectedDate = dates[0] || ''; this.executionDate = this.selectedDate; this.load(); }, error: () => { this.loading = false; this.error = 'No fue posible obtener las predicciones guardadas.'; } }); }
  selectHorizon(horizon: PredictionHorizon) { this.selectedHorizon = horizon; this.load(); }
  load() { if (!this.selectedDate) { this.loading = false; return; } this.loading = true; this.error = ''; this.api.getDashboard(this.selectedDate, this.selectedHorizon).pipe(finalize(() => this.loading = false)).subscribe({ next: data => { this.data = data; this.safetyMargin = data.margen_seguridad; }, error: error => this.error = this.errorMessage(error, 'cargar las predicciones') }); }
  runPrediction() { if (!this.isAdmin || !this.executionDate) return; this.running = true; this.error = ''; this.success = ''; this.api.run(this.executionDate, this.safetyMargin).pipe(switchMap(() => forkJoin({ data: this.api.getDashboard(this.executionDate, this.selectedHorizon), dates: this.api.getDates() })), finalize(() => this.running = false)).subscribe({ next: result => { this.data = result.data; this.dates = result.dates; this.selectedDate = this.executionDate; this.safetyMargin = result.data.margen_seguridad; this.success = `ETL y predicción completados para ${this.formatDate(this.executionDate)}.`; }, error: error => this.error = this.errorMessage(error, 'ejecutar la predicción') }); }
  predictionValue(item: SupplyRecommendation) { return this.selectedHorizon === 3 ? item.prediccion_3_dias : this.selectedHorizon === 7 ? item.prediccion_7_dias : item.prediccion_14_dias; }
  riskPercent(item: SupplyRecommendation) { const expected = this.predictionValue(item) * (1 + this.safetyMargin); return expected > 0 ? Math.min(100, item.compra_recomendada / expected * 100) : 0; }
  isCritical(item: SupplyRecommendation) { return item.estado === 'REQUIERE_COMPRA' && this.riskPercent(item) >= this.riskThreshold; }
  formatDate(date: string) { const [year, month, day] = date.split('-'); return `${day}/${month}/${year}`; }
  exportCsv() { if (!this.selectedDate) return; this.api.exportCsv(this.selectedDate, this.selectedHorizon).subscribe({ next: blob => { const url = URL.createObjectURL(blob); const link = document.createElement('a'); link.href = url; link.download = `predicciones-${this.selectedDate}.csv`; link.click(); URL.revokeObjectURL(url); }, error: error => this.error = this.errorMessage(error, 'exportar el reporte') }); }
  private matchesState(item: SupplyRecommendation) { return !this.state || (this.state === 'CRITICO' ? this.isCritical(item) : item.estado === this.state); }
  private errorMessage(error: any, action: string) { const message = error?.error?.message; if (error?.status === 403) return 'Tu usuario no tiene permisos para ejecutar esta operación.'; if (message?.includes('No ETL features')) return 'La fecha elegida no tiene todos los datos operativos necesarios para el ETL.'; return message || `No fue posible ${action}. Verifica que Spring Boot y FastAPI estén disponibles.`; }
}
